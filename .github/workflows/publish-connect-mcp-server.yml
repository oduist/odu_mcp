name: Publish Connect MCP server

on:
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/publish-connect-mcp-server.yml"
      - "addons/connect_mcp/deploy/connect_mcp_server/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: Docker Hub image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: oduist/connect_mcp_server
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}
      - uses: docker/build-push-action@v7
        with:
          context: addons/connect_mcp/deploy/connect_mcp_server
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
